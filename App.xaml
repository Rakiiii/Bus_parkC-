<Application
    x:Class="Bus_park.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bus_park"
    RequestedTheme="Light">

    <Application.Resources>
        <Style TargetType="Button" x:Key="ButtonStyle">
            <Setter Property="FontSize" Value="22" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Background" Value="{StaticResource SystemControlChromeMediumLowAcrylicElementMediumBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource SystemControlChromeMediumLowAcrylicElementMediumBrush}" />
            <Setter Property="BorderThickness" Value=" 1 " />
            <Setter Property="Height" Value="50" />
            <Setter Property="Width" Value="720" />
        </Style>
    </Application.Resources>
</Application>
